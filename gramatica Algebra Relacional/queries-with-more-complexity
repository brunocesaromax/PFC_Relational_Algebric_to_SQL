SIGMA a1>2 (PI a1,a2 (SIGMA a1>10 (SIGMA a2>5(REL1 JOIN a2 = a5 (REL2 UNION (REL3 NATURAL_JOIN (REL4 INTERSECTION (REL5 JOIN a7>a1 AND a2=1 PI a1,a2,A3 (REL6)))))))))

SIGMA a1>2 (PI a1,a2 (SIGMA a1>10 (SIGMA a2>5(REL1 JOIN a2 = a5 (REL2 UNION (REL3 NATURAL_JOIN (REL4 INTERSECTION (REL5 JOIN a7>a1 AND a2=1 PI a1,a2,A3 (REL6)))))))))

SIGMA a1>2 (PI a1,a2 (SIGMA a1>10 (SIGMA a2>5(REL1 JOIN a2 = a5 REL2 UNION REL3 NATURAL_JOIN REL4 INTERSECTION REL5 JOIN a7>a1 AND a2=1 PI a1,a2,A3 (REL6)))))

SIGMA a1>2 ( PI a1,a2 (CLIENTE1) JOIN a2 = a5 ( CLIENTE2 UNION (CLIENTE3 NATURAL_JOIN CLIENTE4) ) )

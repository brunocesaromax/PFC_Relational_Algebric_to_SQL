%token NUMBER;
  
expression :
	term
;
  
term :
	term '+' factor
	| term '-' factor
	| factor
;
  
factor :
	factor '*' primary
	| factor '/' primary
	| primary
;
  
primary :
	NUMBER
	| '(' term ')'
	| '-' primary
;
